{
  "//": [
    "Typechecks OUR sources only. The pinned rubrc modules and vite's query",
    "imports are declared as `any` in src/rubrc-modules.d.ts rather than",
    "checked: they are a third-party checkout at a pinned commit, we do not",
    "get to fix their types, and their errors would bury ours. This also lets",
    "`npm run typecheck` run in a tree where `.rubrc/` has not been built."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "types": []
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts"]
}
