# Enums and error discipline

**Date:** 2026-09-06
**Status:** Decided 2026-09-06. The nine rulings below are taken; section 5 keeps the reasoning as the record and is not re-litigated. PRs execute in the order of section 4.
**Repo:** `impresspress` only. Two items in this phase's remit are upstream (`wafer-core`) and are recorded for Phase 4 rather than done here (5.3, 5.4).
**Origin:** Phase 3 of `docs/CODE_REVIEW_2026-09-05.md` (section 7); themes T4, T5; bugs B11, B20, B21, B22, B23.
**Base verified against:** `42684d0d` (Phase 2 PR 7, #25, merged). Paths are relative to `crates/impresspress-core/src/` unless another crate is named. Every line number below was re-read from this tree; the review's numbers and both earlier specs' numbers are stale and are not carried forward.

## Decisions taken

These override the draft's recommendations wherever they differ. Section 5 is the reasoning that produced them and stays as the record.

1. **B11 — accept the finding.** It does not reproduce; `pending_review` and `pending` are values of two different columns, each consistent. Close it with a regression test pinning that the two columns are distinct and each round-trips, and fix the real residue: `ApprovalStatus`'s five variants describe the product column while the enum is bound to `SellerAccount`, where only two are reachable. No wire change. The review report's B11 row becomes "not reproducible; closed by test" rather than being deleted.
2. **Money — the column rename is deferred; the correctness half is done now.** Draft PR 7 (migration 021, dual write, wire rename) is dropped; draft PR 8 is kept and expanded — unify the two decimal parsers into `money::Decimal` with `to_minor(currency)`, and stop `total_cents`/`amount_cents` and `user_id`/`buyer_user_id` being published twice, so the hand-sync drift risk goes without a migration. The stored values are already correct minor units, so `_cents` is a misleading name rather than wrong data, while the two parsers disagreeing is a live correctness bug. See 2.3, which keeps the staged plan under a "deliberately deferred" heading.
3. **`errors.rs` — keep and shrink.** It is the only producer of the `error.code` the SDK reads, so it stays. `status_code()` is deleted (no callers, and it says 413 where 429 ships). The 847 `err_*` sites are **not** migrated; that is its own phase, and saying so here is part of the work.
4. **`get_bool` lives in `config_vars.rs`**, not upstream. Upstream is Phase 4 and wafer-run is pinned.
5. **Truth table A (`1`, `true`, `yes`, `on`) wins**, because it is the superset: no deployment whose config works today stops working.
6. **`context_type` stays a `String`.** It is not a closed set.
7. **Messages' role becomes a full `EntryRole` enum owned by the messages block**, not an extra arm in llm's parser. B20 is precisely the bug stringly-typing produces — the block documents `agent`, llm replays it as a user turn. The enum is the fix; the arm is the shim.
8. **Sequencing — ten PRs** (section 4). The draft's eleven minus the money migration, with its PR 8 becoming PR 7 as expanded in ruling 2. The three T4 sweep PRs stay separate: error-swallowing is the theme with the most fail-open behaviour left in the tree, and it is why several Phase 0 bugs existed. They are not folded into the enum PRs, which already move snapshots.
9. **Third snapshot gate.** `dev.tools.json` embeds the products contract schemas and only regenerates under `--features block-dev`. Every PR that moves a products contract runs the block-dev snapshot pass and either shows that file's diff or states it is unchanged.

## Goal

A stored value that is one of a fixed set is a Rust enum with one definition; a failure is reported with the code that describes it. Concretely, when this phase is done:

1. No block writes or compares a bare string literal for a status, role or kind that its own `contracts.rs` already defines an enum for. `grep -rn '"pending_review"\|"not_started"\|"dead_letter"\|"assistant"' crates/impresspress-core/src/blocks --include=*.rs` finds hits only in `contracts.rs` doc comments, migrations and tests.
2. Every enum'd column has exactly one decode door. `contracts::enum_column` (`blocks/products/contracts.rs:1276`) and `repo::offers::wire_enum` (`blocks/products/repo/offers.rs:34`) collapse into one helper with an explicit empty-column policy, and each content block gets the same.
3. `crud::db_error` and `http::require_row` exist, and no handler hand-writes `if e.code == ErrorCode::NotFound { err_not_found(..) } else { err_internal("Database error", e) }`. The count of that shape drops from **62** to 0 outside the two helpers, and `PermissionDenied` no longer arrives at a client as 500.
4. `errors::ErrorCode::status_code()` is gone (it has zero non-test callers and disagrees with the mapping that actually runs), and `rate_limit::rate_limited_response` no longer emits a `"[code] message"` prefix.
5. `SELLER_APPLICATION_FEE_BPS`, `PLATFORM_COUNTRY` and every boolean config flag are read through one function each, with one truth table.
6. B20, B21, B22, B23 are closed by construction, each with a test that fails on this tree. B11 is closed as **not reproducible** with a regression test that pins why (1.8).
7. Both snapshot gates plus `dev.tools.json` are byte-identical for every PR except the ones section 3 names, and each of those diffs is listed line by line in its PR.

## Why

The review's themes hold, but Phases 1 and 2 moved most of the evidence and closed some of it. What is left is sharper than the review describes in two places and much larger in one.

- **Products has already done half the enum work.** `contracts.rs` carries 25 enums (`:35-1336`), `OrderStatus` and `ReconciliationStatus` are written *and* read as enums (`repo/purchases.rs:263, 306, 409, 1143-1150`; `contracts.rs:2312, 2324`), and route schemas derive from the contract types (`routes.rs:30-31` imports `request_schema_of`/`response_schema_of`). The review's "48 drifted schema literals" is closed. What remains is a short, specific list (1.1).
- **The error problem is 8× the review's estimate.** The NotFound→404 / everything-else→500 shape appears at **62 sites across 27 files**, 34 of them with the literal context `"Database error"`, and none of them re-checks `PermissionDenied` — so a WRAP row-guard denial reaches the client as `500 Internal server error (ref: …)`. `err_internal`'s `context` argument never reaches the wire (`wafer-block/src/response.rs:77-89`), so all 62 are client-indistinguishable.
- **The money rename is a published wire change, not a column rename.** All eight `_cents` columns live on one table, `impresspress__products__purchases` (`migrations/001_products_schema.sqlite.sql:76-77`, `005_commerce_v2.sqlite.sql:200-204`, `013_order_shipping.sqlite.sql:4`), but they are also published in `products.openapi.json` (`:5561-5783`), typed in the SDK (`packages/impresspress-js/src/services/extensions.service.ts:745-752, 792-797, 854-860`), asserted in `pipeline.rs:1549`, and asserted in both CI workflows (`ci.yml:1251`, `ci-main.yml:871`). And the migration runner re-runs the whole concatenated corpus on any hash change (`migration_helper.rs:166-221`), tolerating only a duplicate `ADD COLUMN` (`:193-212`), so a bare `RENAME COLUMN` bricks every later migration.
- **Two config keys have two truth tables each.** `WAFER_RUN_SHARED__ALLOW_USER_PRODUCTS` is `{1,true,yes,on}` case-insensitively at `products/stripe.rs:989,1318` (through `configured_bool`, `stripe.rs:558-563`) and exactly `"true"` at `products/handlers/dispatch.rs:24`. `IMPRESSPRESS__PRODUCTS__AUTOMATIC_TAX` is the same split (`stripe.rs:1165,2353` vs `pages.rs:995`). So a value of `"1"` opens the route guard and closes the money path, or the reverse.

## 1. Verified inventory

### 1.1 Products statuses, roles and kinds

Enums that exist, all `#[derive(Serialize, Deserialize, schemars::JsonSchema)] #[serde(rename_all = "snake_case")]`, none with a hand-written `as_str`/`FromStr` except `RefundReason::as_str` (`contracts.rs:972-980`, which duplicates serde): `ApprovalStatus:35`, `FulfillmentKind:45`, `OfferMode:55`, `OfferStatus:62`, `PricingModel:70`, `RecurringInterval:77`, `UsageType:86`, `BillingScheme:93`, `TaxBehavior:100`, `ShippingEstimateUnit:111`, `CheckoutPresentation:121`, `VariableKind:132`, `VariableVisibility:146`, `Condition:186`, `PackageRounding:249`, `AmountRule:260`, `QuantityRule:296`, `StripeMode:525`, `OrderStatus:665`, `ReconciliationStatus:716`, `StripeConnectionState:890`, `RefundReason:965`, `RefundResultStatus:1005`, `ProductStatus:1329`.

Two decode doors, differing on the empty column: `contracts::enum_column` (`:1276-1287`, unknown value → `ErrorCode::Internal` naming the row) and `repo::offers::wire_enum` (`repo/offers.rs:34-43`, empty → a caller-supplied fallback, then the same failure). Only `OrderStatus` and `ReconciliationStatus` use `enum_column`; only `OfferStatus` uses `wire_enum`.

What is still stringly, with every consumer:

| Column / value set | Where it is a `String` today | Sites |
|---|---|---|
| `seller_accounts.status` — `not_started\|onboarding\|restricted\|active\|suspended` (**five**, not the review's four) | `contracts.rs:859 pub status: String` beside `:860 pub approval_status: ApprovalStatus` | Ladder computed 3× — `repo/seller_accounts.rs:187-195` (written `:201`), `:251-259` (`:266`), `:399-407` (`:445`, CAS filter `:424-426`). Fifth literal `"not_started"` at `:230` and `stripe_provider.rs:283`. Reads: `seller_accounts.rs:73,159,533`; `stripe_provider.rs:312,352`; `handlers/sellers.rs:139`; `pages.rs:831,836,841,2874,2876`. Published as bare `{"type":"string"}` at `products.openapi.json:7283-7285` while `approval_status` beside it carries the full enum (`:7212-7221`) |
| `offers.status` — `OfferStatus` exists but never writes | Writes `repo/offers.rs:526,708,735`; CAS expectations `:555,655,673,714`; `expected_status: &str` params `:566-597`; comparisons `:628,657,870,877`; list filter `:849` | Enum→string re-spelled twice: `repo/offers.rs:737-741` and `pages.rs:697-703` (`offer_status_label`) |
| `offers.sync_status` — `not_synced\|syncing\|synced\|failed` | `contracts.rs:482 pub sync_status: String` | `repo/offers.rs:381,410,427,452` |
| `products.status` / `products.approval_status` | Both `String` on `ProductView` with hand-written `#[schemars(extend("enum" = [...]))]` — `contracts.rs:1351-1354` and `:1380-1383` | Literal writes `handlers/product.rs:403,749,911,1017,1030`; `handlers/sellers.rs:92,98`. `ProductStatus` reaches the DB only through serde on `CreateProductRequest.status` (`contracts.rs:1694`) / `UpdateProductRequest.status` (`:1750`) via `columns<T>` (`:1315-1317`) |
| `purchases.provider_payment_status` (Stripe PaymentIntent vocabulary) | bare strings | `repo/purchases.rs:70,631,857,1024-1027,1097,1107,1143-1147`; schema `contracts.rs:2185` |
| `purchases.subscription_status` | bare strings, and **two spellings of one state**: `"cancelled"` at `repo/subscriptions.rs:397` vs `"canceled"` everywhere else, reconciled only by `repo/mod.rs:38-44 subscription_status_rank` | `repo/subscriptions.rs:31,302,367,376,457,512`; `repo/purchases.rs:649,870,1651-1654`; `stripe.rs:3559,3563` |
| `disputes.status` | `repo/disputes.rs:29-45 supported_status`/`is_closed` | re-spelled in the schema at `contracts.rs:2938` |
| `refunds.status` — includes a local-only `provider_succeeded` with no enum | `repo/refunds.rs:112,118,164,219-223,276,336-341,404,426,449` | read `purchase.rs:303-307,485,661,679,709`; `stripe_provider.rs:704` |
| Stripe event `status` — `pending\|processing\|failed\|processed\|dead_letter` | consts `stripe.rs:43-48`, used as values and patterns (`:109,147-163,174,232,252,286-288,323,435,481,489`) | re-spelled `handlers/provider.rs:69`; schema `contracts.rs:1155`; route schema `routes.rs:301`; SDK `extensions.service.ts:621-626` |
| Provider-operation `status` — `pending\|processing\|failed\|succeeded\|dead_letter` (differs from the above in one variant) | `repo/provider_operations.rs:82,163,187,205-208,244,283,292,307,338,349,406-411,473-477` | re-spelled `handlers/provider.rs:104`; schema `contracts.rs:1210`; `routes.rs:312`; SDK `:652-657`; published raw as `String` on `ProviderOperationSummary` (`handlers/provider.rs:37`) |
| Stripe webhook event types (17) | `&str` match at `stripe.rs:3156` inside `handle_webhook` (`:3018-4142`, **1125 lines**) | the same 17 re-spelled at `pages.rs:2753-2766` (the review's `2596-2609` is stale); the PI subset a third time at `stripe.rs:3380-3388` |

Order-status literals: the review's "23 in `repo/purchases.rs`" does not reproduce — 19 distinct values, 34 occurrences, and they belong to **five unrelated vocabularies** (order lifecycle, reconciliation, PaymentIntent, Checkout `payment_status`, subscription/dispute). The order lifecycle itself is fully enum'd with **one** leak: `repo/purchases.rs:1842` filters `status == "completed"` as a bare string.

### 1.2 Content-block kinds and roles

**Messages.** `context_type` (`messages/contracts.rs:33-34`), entry `kind` (`:61`, `#[serde(default = "default_entry_kind")]` → `"message"`, `:82-84`) and entry `role` (`:64`, `#[serde(default)]` → `""`) are all `String`, all documented as fixed sets in doc comments only (`:32`, `:59`, `:62`), and **none is validated** — `rest::add_entry` (`messages/rest.rs:156-182`) hands `&body.kind`/`&body.role` straight to `service::add_entry` at `:170-171`, which writes them verbatim (`service.rs:180-183`). The columns are plain `TEXT`, no CHECK (`migrations/001_messages_schema.sqlite.sql:16-17, 45-47`). The value sets are re-spelled a fourth and fifth time in the route descriptions (`messages/mod.rs:141, 186, 187`).

Two facts that constrain the design:

- **There was a 400-guard on `role` and it was deliberately removed.** `messages/rest.rs:529-558` is a regression test locking in that `assistant` and `system` must be *accepted*. Any enum must therefore include `assistant`, and the PR that tightens `role` has to reconcile with that test rather than delete it.
- **An entry with no role is invisible to the model.** `role`'s default is `""` (`contracts.rs:63`) and `llm/routes/chat.rs:72` filters out any entry whose role is empty. So the messages block's own default silently drops the entry from LLM history.

`messages.openapi.json` publishes `kind` at `:318-322` and `role` at `:324-328` as bare `{"type":"string"}` with the value list in `description` only, and the query params at `:238-241`, `:265-268`. **The snapshot contains no `enum` anywhere**, so typing these is snapshot-visible.

**B20.** `llm/routes/chat.rs:38-44` (the review's `:44-51` is stale) maps `"assistant"`→`Assistant`, `"system"`→`System`, **everything else including `"agent"`**→`ChatRole::User` (`:43`); `history_to_messages` (`:68-79`) feeds it. Two tests lock the wrong behaviour in: `chat.rs:399-403` covers only the three known roles and `chat.rs:406-410` asserts `"tool"`, `""`, `"random"` → `User`; `"agent"` is tested neither way.

llm writes only `"user"` (`chat.rs:127`) and `"assistant"` (`chat.rs:243`, `streaming.rs:131`), through `messages_create` (`llm/mod.rs:311-343`, which also hardcodes `"kind": "message"` at `:333`). **No code in `crates/` writes `"agent"`** — the only three repo-wide literals are `messages/pages.rs:38` (the renderer's `"agent" | "assistant"` arm), `messages/pages.rs:349` (the composer dropdown, which offers `agent` and **not** `assistant`) and an unrelated `tickets/models.rs:621`. So B20 is reachable only through the messages composer, but that is a live path: a human posts `role=agent` into a thread and llm replays it as the user's own turn.

`llm.openapi.json` contains no `role` field at all (the chat body publishes only `thread_id`/`message`/`provider`/`model`), so **the llm half of B20 is free of any published-contract cost.** `ChatRole` is upstream (`wafer_core::clients::llm`, imported at `chat.rs:14-17`); its four variants (`System`, `User`, `Assistant`, `Tool`) are visible through the exhaustive matches at `llm/providers/openai.rs:203-209` and `anthropic.rs:164-168`, but the definition could not be read, so **adding an `Agent` variant upstream is not assumed to be possible** — the design converts instead.

**Files.** Object `status` is `String` (`files/repo/objects.rs:43`, doc `:40-42`), two values, seven production literal sites: `repo/objects.rs:73-78` (`complete_filter`), `:133` (`insert_pending`), `:145` (`mark_complete`), `:211` (stale-pending sweep), `:249` (`search_completed`), `storage/search.rs:60`, `storage/mod.rs:87`. Column is plain `TEXT`. **`files.openapi.json` does not publish it** (the file is 4.5 KB and contains no `status`), so typing it moves no snapshot — though `files/contracts.rs:210-226` asserts `status` is among the published record columns. Nothing else in files is stringly: `public` is a real `bool` (`repo/buckets.rs:52`) after Phase 2's B13 fix.

**Legalpages.** `doc_type` is `String` (`repo/documents.rs:36`, `:77`, `:87`; `service.rs:24`) and **still accepted unvalidated from request bodies** — the review's `mod.rs:302-306` is now `legalpages/mod.rs:311-317` (the private `CreateDoc`) forwarding at `:328`, and its `pages.rs:511-519` is now `pages.rs:437-446` (`SaveRequest`) forwarding at `:479` and `:522`. Phase 2 added the repo layer but no validation: `repo::documents::of_type` (`:108-114`) filters on whatever string it is given, so `{"doc_type":"cookies"}` creates a row no route can serve (the public routes are hardcoded to `terms`/`privacy`, `mod.rs:681-684`).

Document `status` literals: `repo/documents.rs:176`, `:184`, `:205`; `pages.rs:468`, `:495`, `:539`. The `"terms"`/`"privacy"` label mapping is **three copies that disagree with each other**: `mod.rs:237-241` falls back to "Privacy Policy", while `pages.rs:50-54` and `pages.rs:96-100` fall back to "Terms of Service" — so one unvalidated `doc_type` gets two different labels on two pages. `legalpages.openapi.json` (1.6 KB, `PATCH` only) publishes neither `doc_type` nor `status`, so typing them moves no snapshot; the `POST` that accepts `doc_type` is not in the snapshot at all.

### 1.3 The remaining T4 sites

Phase 2 closed more than its own non-goals claimed. Verified **fixed**, and therefore out of scope: legalpages' lookup-error-creates-a-draft (now `pages.rs:461-471`, with `find_current_doc` returning `Result` at `:31-38` and a named regression test); legalpages' count-error-reseeds (now `mod.rs:438-439` through `documents::count`, regression test `mod.rs:893-915`); `userportal/pages/sessions.rs:131-167` (revoke now propagates all three failures); `reset_password.rs:91-99` and `change_password.rs:89-101` (token-revocation swallows, both with regression tests); `admin/pages/blocks.rs:250-252` (the `let _ = set_enabled` toggle, regression module at `:505-610`); `files/pages_user/cloudstorage.rs:139-144` (quota failure now returns `server_error_response`); `products/repo/subscriptions.rs:185-193` (`platform_subscription_exists` returns `Result<bool>` and all four webhook callers three-way branch at `stripe.rs:3500-3510, 3575-3585, 3631-3641, 3689`). **Legalpages now has zero swallow sites.**

Still open, grouped as the PRs will take them:

*Group 1 — SSR page renderers (~25 sites).* files `pages_user/buckets.rs:136-139, 149-152`, `pages_user/objects.rs:214-217`, `pages_user/cloudstorage.rs:115-118` (all four now log, none surfaces); files `pages_admin.rs:173-201` (five zeroed stat tiles), `:294-297`, `:425-428`, `:518-521`; plus two the review missed on the **JSON API**, `files/storage/admin.rs:14, 18` (`unwrap_or(0)`). messages `pages.rs:75-76`, `:175-176`, `:191-194` — still the only three in the block and still **unlogged**, while the correct pattern sits four lines away at `pages.rs:162-164`. admin `pages/dashboard.rs:101-106` — the review's five scattered lines are now **six contiguous `unwrap_or_default()`/`unwrap_or((0,0))` after a `futures::join!`**, still unlogged, rendering a healthy-looking dashboard of zeros during an outage; `pages/network.rs:37-39, 153-155`; `pages/storage.rs:84-86`; `pages/blocks.rs:44` (on failure every block renders enabled); `pages/permissions.rs:126, 439` (renders "no custom grants", which reads as "no one has extra access"). llm `pages.rs:366` (settings overrides), `:112` (thread sidebar), `:117` (entries), `:514-518` (`load_models`) — each carries an in-code comment deferring it to "Phase 3 (T4)".

*Group 2 — llm persistence.* `llm/mod.rs:338-342` — `messages_create` swallows twice in four lines (`if let Ok(buf)` and `.ok()`); only the serialization failure logs (`:327-330`). `chat.rs:127` `let _ =` on the **user's own turn**, so the message can vanish before the model is called. `streaming.rs:131` `let _ =` on the assistant turn. `chat.rs:243-252` binds the result but only to dig out an id, publishing `message_id: ""` in a 200 on failure. `llm/mod.rs:362` `messages_list(..).unwrap_or_default()` is the worst of the set: it builds the **model history** (`chat.rs:130`), so a messages outage silently sends a context-free prompt to a paid provider.

*Group 3 — products webhook and compensation honesty.* `repo/purchases.rs:369` `let _ = delete_with_line_items` (compensating rollback, `delete_with_line_items` itself returns `Result` at `:377-392`). The five `let _ = mark_checkout_failed` at `stripe.rs:1182, 1213-1218, 1230-1235, 1241-1246, 1269-1274`, **plus three the review missed**, `let _ = repo::payment_links::mark_error` at `stripe.rs:2385, 2401, 2427`. `stripe.rs:4311-4326` `user_owns_product` → `Err(_) => false` at `:4324`, which the caller at `:1049-1056` turns into "You must sign in and own the required product". `repo/subscriptions.rs:467-486` `find_user_by_stripe_sub -> Option` (documented as deliberately preserving the old behaviour), callers `stripe.rs:3531, 3664` — a DB blip skips the addon-total sync and the outbound `products.subscription.updated` while the webhook still reports success. `repo/subscriptions.rs:497` `active_plan_exists -> bool`. `repo/purchases.rs:1854-1884` `line_item_exists_for_product -> bool` ending in `matches!(rows, Ok(rows) if ..)`, so a failure reads as "doesn't own it". **`default_template_id` survived Phase 2 under a new name**: `repo/group_templates.rs:33` and `repo/product_templates.rs:12`, both still `-> Option<String>` collapsing errors into `None` (callers `handlers/group.rs:166`, `handlers/product.rs:954`), with a tombstone comment at `handlers/mod.rs:49-52`.

*Group 4 — auth handler collapses.* Ten still open, three of them defensible: `auth_ui/api/me.rs:40-42`, `api_keys.rs:180-182` and `:203-205`, `change_password.rs:41-44`, `refresh.rs:118`, `verify.rs:58-63`, `reset_password.rs:38-39`, `userportal/pages/admin_buttons.rs:252-254`. Deliberate anti-enumeration constant responses, to be left alone with a comment rather than "fixed": `verify.rs:120-122` (rationale at `:113-118`) and `forgot_password.rs:26`.

Sweep totals for scale: `let _ =` in `blocks/` is **66 raw** including tests and including stream plumbing that is not an error (12 in `storage.rs` are `sink.*`, 6 of llm's 14 are SSE sends). Real per-block: llm 7, admin ~13 (several test-only), products 10, auth_ui 6 (4 test-only), dev 3, vector 2, tickets 2, auth 2, and **zero in files, messages, legalpages and userportal**. `unwrap_or_default()`/`unwrap_or(0)`/`.ok()` token counts as a ceiling: products 77, admin 40, llm 24, dev 19, tickets 16, auth_ui 11, auth 11, vector 9, files 8, userportal 7, legalpages 6, messages 1.

**Runtime sites, all three still open and all three Phase 4:** `pipeline.rs:520-521` `let _ = request_logs::insert` (moved out of raw `db::create`, now with a `Queued` alternative at `:523-525`, but the Inline path still discards); `impresspress-cloudflare/src/config_service.rs:26-28` — the `set` no-op **cannot** be fixed here because the trait method returns `()` (`wafer_core::interfaces::config::service::ConfigService`), which is an upstream signature change; `impresspress-bundle/src/bundle/mod.rs:289` and `:420`, both in functions that already return `Result<()>`, plus a swallowed `read_dir` at `:282-284`.

### 1.4 The error model

`errors.rs` (256 lines) exports `ErrorCode` (24 variants, `:4-42`), `as_str` (`:48-75`), `status_code` (`:80-111`), `Display` (`:114-118`), `error_response` (`:130-135`) and `pub(crate) impresspress_error_code_to_wafer` (`:138-172`, called from outside the module at `rate_limit.rs:299`).

- `status_code()` has **zero non-test callers** (exhaustive grep; the other `status_code()` hits in the repo are `resp.status_code()` on HTTP responses). It also disagrees with the mapping that runs: `QuotaExceeded | FileTooLarge => 413` (`errors.rs:102`) while the same two map to `ResourceExhausted` (`:160-162`) → **429** (`wafer-block/src/http_codec.rs:281`, reached via `resolve_error_status`, `:303-305`). `errors.rs:205-206` pins the unreachable 413.
- The `"[code] message"` prefix the doc comment at `errors.rs:126-129` calls "gone" survives at exactly one site: `rate_limit.rs:296-312` (`rate_limited_response`, called from `:342`), which hand-builds the `WaferError` so it can attach `Retry-After` and therefore attaches **no** `error.code` detail meta — the prefix is that response's only machine-readable code. The SDK reads `body.code` into `ImpresspressError.detailCode` (`packages/impresspress-js/src/http-client.ts:162-169`, `error.ts:14-38`).
- Six styles, current counts inside `blocks/`: `errors::error_response` 36 non-test sites, **35 of them in `auth_ui`** (`api/refresh.rs` 12, `signup.rs` 6, `reset_password.rs` 5, `change_password.rs` 4, `login.rs` 3, `api_keys.rs` 2, `me.rs` 2, `bootstrap.rs` 1) plus one at `files/pages_user/cloudstorage.rs`; `http::err_*` **847** (`err_internal` 385, `err_bad_request` 252, `err_not_found` 104, `err_internal_no_cause` 44, `err_forbidden` 38, `err_unauthorized` 17, `err_conflict` 7); raw `OutputStream::error` 20 non-test; `Result<_, String>` 83 (mostly `dev`, `tickets/models.rs`, `products/offer_pricing.rs`); `Result<_, OutputStream>` 50 (`crud.rs` 12, `admin/ops.rs` 8, …); `-> bool` 86 declarations, of which the success/failure-shaped swallowers include `auth/repo/jwt_blocklist.rs:64`, `products/stripe.rs:4311 user_owns_product`, `products/repo/subscriptions.rs:497 active_plan_exists`.
  **A seventh style arrived in Phase 2**: `auth::repo::RepoError` (`auth/repo/mod.rs:36-44`), `NotFound | Db(String)`, which discards the wafer code — so a WRAP denial inside an auth repo function is indistinguishable from a decode failure. 38 repo functions now return `Result<Option<_>>`, split between `RepoError` (auth) and `WaferError` (products, dev).
- `crate::http` (`http.rs`) **defines no `err_*`**: `:14-17` re-exports them from `wafer_block`. Its only local additions are `redirect` (`:23-28`) and `mod conditional` (`:41-163`). `err_internal` mints a correlation id, logs `context` + `error`, and returns only `"Internal server error (ref: <id>)"` (`wafer-block/src/response.rs:77-89`).
- **The 62 hand-mapped sites** (NotFound arm + collapsing tail within six lines, tests excluded), by file: `crud.rs` 4 (`:120,162,182,232`), `products/` 28 (`pages.rs:575,2075,3268`; `purchase.rs:234,266,284,367,424`; `handlers/offers.rs:79,100`, `commerce.rs:91,190,262`, `sellers.rs:18`, `catalog.rs:64`, `provider.rs:55`, `product.rs:163,322,363,383,453,710,1066`; `stripe.rs:966,979,1026`), `admin/` 6 (`ops.rs:311`, `mod.rs:777`, `iam.rs:109,145,274,415`), `vector/pages.rs` 5 (`:299,331,361,496,724` — these map a *block-missing* NotFound, documented at `vector/pages.rs:75`), `llm/` 5 (`mod.rs:434`; `routes/providers.rs:191,230,255,285`), `messages/` 3 (`rest.rs:117,129`; `pages.rs:163`), `legalpages/` 3 (`mod.rs:413,426`; `pages.rs:533`), `files/` 4 (`storage/objects.rs:130`, `share.rs:152`, `cloud.rs:146,152`), `tickets/` 2 (`rest.rs:219`, `pages.rs:185`), `dev/generations_api.rs` 2 (`:67,108`). The three per-block private helpers that already abstract it, each with a different fallback context, are `handlers/sellers.rs:18`, `handlers/provider.rs:55`, `handlers/product.rs:163`, `handlers/offers.rs:100`.
- `crud.rs` (296 lines) carries the shape four times (`:119-125`, `:161-167`, `:181-187`, `:232-235`) and `err_internal("Database error", e)` seven times (`:108,123,145,148,165,185,235`). `path_id` (`:48-57`) is the intended single empty-id guard and has 6 callers, 3 of them inside `crud.rs` itself — against **51** hand-rolled `err_bad_request("Missing … ID")` copies elsewhere, which also drift on casing.
- **Neither `crud::db_error` nor `http::require_row` exists** (zero grep hits repo-wide). There is no `impl From<..> for OutputStream` anywhere in this repo, and none upstream — orphan rules allow only `From<LocalType> for OutputStream`, which is exactly the shape needed and is unclaimed.
- Test coverage of all this: **no test asserts the `"Internal server error (ref: …)"` body or the `"Database error"` context**, so the 62 sites are unpinned. `products/tests/*` do assert heavily on `ErrorCode::NotFound` through `output_is_error` (~50 assertions), and there are two same-named helpers with different contracts: `test_support.rs:1605` (string-compares the wafer code's `Debug`) and `products/tests/harness.rs:183` (typed). `test_support::output_http_status` (`:1544-1553`) resolves through `wafer_block::http_codec`; `messages/rest.rs:270-277` is a byte-equivalent local duplicate.

### 1.5 Money

- **Eight `_cents` columns, all on `impresspress__products__purchases`**: `total_cents`, `amount_cents` (both `001_products_schema.sqlite.sql:76-77` / `.postgres.sql:70-71`), `subtotal_cents`, `discount_cents`, `tax_cents`, `platform_fee_cents`, `refunded_total_cents` (`005_commerce_v2.sqlite.sql:200-204` / `.postgres.sql:171-175`), `shipping_cents` (`013_order_shipping.*.sql:4`). No `_cents` column exists on any other table. `_minor` columns exist on `offers`, `line_items` (`005_commerce_v2.sqlite.sql:84,219-223`), `refunds` (`008_refund_ledger.sqlite.sql:8-9`) and `disputes` (`015_dispute_ledger.sqlite.sql:10`).
- Occurrence counts: 210 `_cents` matches inside `blocks/products/`, of which ~98 are non-test; per file `contracts.rs` 45, `repo/purchases.rs` 26, `pages.rs` 11, `purchase.rs` 7, `handlers/commerce.rs` 6, `stripe.rs` 2, `stripe_provider.rs` 1, plus ~94 in `tests/`. The review's "52 sites" reproduces under no counting rule I tried.
- **The Rust side is already `_minor`.** `repo/purchases.rs:265-272` writes `("total_cents", json!(snapshot.amounts.total_minor))`; only the column names are `cents`. And every published view is *hand*-projected (`contracts.rs:2619-2636`), so **the column name and the wire name are independent** — either can be renamed without the other.
- **`total_cents` / `amount_cents`.** One writer pair each (`repo/purchases.rs:265-272`, `:838-845`) writing the same expression into both. 14 non-test readers of `total_cents`; exactly **one** reader of `amount_cents` — `contracts.rs:2630`, feeding `PurchaseView.amount_cents` (`:2165`, doc-commented "Legacy amount column, in minor units. Prefer `total_cents`"). Nothing compares them, so a pre-005 row where they disagree publishes a wrong `amount_cents` silently.
- **`user_id` / `buyer_user_id`.** `user_id` from `001` (`sqlite.sql:73`, indexed `:92-93`); `buyer_user_id` added by `005_commerce_v2.sqlite.sql:192`. One writer, both from `snapshot.buyer_user_id` (`repo/purchases.rs:241-248`). **The read sets differ**: order lists filter `user_id` (`purchase.rs:41,63`; `repo/purchases.rs:1536,1835`; `pages.rs:3747`) while the Billing-Portal lookup filters `buyer_user_id` (`repo/purchases.rs:537`) and per-row ownership reads `buyer_user_id` with a `user_id` fallback (`purchase.rs:242-246`; `repo/purchases.rs:513-517`; `pages.rs:3276-3280`). A divergent row would be listed under one identity and access-checked under another. The published doc string (`contracts.rs:2131-2133`) claims `user_id` is empty for guest orders; the writer actually mirrors `""` into both.
- **Two decimal parsers, and the exponent-aware one is dead.** `money::parse_amount_minor` (`money.rs:41-94`) is currency-aware (`currency_exponent`, `:24-33`, with the 0-decimal and 3-decimal tables), rejects negatives (`:47-49`), rejects scientific notation (`:56-57`, tested `:156`), and refuses extra non-zero fractional digits (`:63-70`). It has **zero production callers** — only its own tests. The live parser is `offer_pricing::Decimal::parse` (`offer_pricing.rs:65-103`), which has no notion of currency, accepts negatives (`:67-71,91-93`), caps scale at 9 (`:27,82-86`), and defers the whole-minor-unit check to `multiply_minor` (`:139-155`). `money.rs`'s live surface is `normalize_currency` (12 call sites), `format_amount_minor` (`pages.rs:20`) and `currency_exponent` (`pages.rs:3323`, serialized to the refund JS at `:3328`).
- **Migration mechanics.** 20 files, registry `migrations/mod.rs:79-100` (sqlite) and `:104-133` (postgres, `#[cfg(any(feature = "postgres", test))]`, aliased to `&[]` without the feature at `:140-141`); registered at `products/mod.rs:395-409` via `migration_helper::lifecycle_init`. `apply_migrations` (`migration_helper.rs:93-105`) **joins the whole ordered list into one string** and `apply_if_blessed` (`:153-230`) hashes it; a changed hash re-runs every statement from 001 (`:166-221`). The only tolerated re-run failure is a duplicate `ADD COLUMN` (`:193-212`, `is_alter_add_column:372`, `is_duplicate_column_error:392`). **There is no `RENAME COLUMN` or `DROP COLUMN` precedent anywhere in the repo**, and 020's header (`020_normalize_blank_deleted_at.sqlite.sql`) spells out what an un-tolerated failure costs: the hash is never stamped, every later boot re-runs and re-fails, and on Cloudflare `strict_init_all_blocks` turns that into a 500 per request. D1 uses the sqlite list (`migrations/mod.rs:136-137`).
- **Consumers outside `crates/*/src`:** SDK `extensions.service.ts:745-752, 792-797, 854-860` (three order interfaces) and `:734` (`buyer_user_id`). `examples/tests/*.spec.ts`: zero hits. `crates/impresspress-web/tests/e2e/*`: zero `_cents`, zero `buyer_user_id`. `examples/products/*/commerce.fixture.json`: `_minor` only. Embedded JS (`products/assets/storefront.js:402-411`, the inline refund script at `pages.rs:3256`): `_minor` only.
- **Two hard test dependencies on the current names:** `pipeline.rs:1547-1550` asserts `refunded_total_cents` appears in the published WebMCP `outputSchema`; `ci.yml:1251` and `ci-main.yml:871` assert `shipping_cents` exists in `information_schema.columns` after applying the postgres migrations.

### 1.6 Boolean config flags

`config_client` is not a local module — it is an alias for `wafer_core::clients::config` (`pipeline.rs:9`, `auth/config.rs:17`, `auth/mod.rs:32`, `tickets/public.rs:6`), whose entire API is `get` / `get_default` / `set` (`wafer-core/src/clients/config.rs:20-37`). **There is no `get_bool` and adding one is an upstream change** (decision 5.4). `get_default` swallows every error, including WRAP denials, into the default.

Three truth tables in `blocks/`:

- **A — `{1,true,yes,on}`, trimmed, case-insensitive:** `products/stripe.rs:558-563` (`configured_bool`, callers `:989,1165,1318,2353`); `tickets/config.rs:201-206` (caller `:130`); `tickets/service.rs:620-631` (a third copy of the same logic for the *same key*); `tickets/public.rs:624-631` (form field); and two near-misses that drop the trim/lowercase — `vector/contracts.rs:206-207` (case-sensitive, untrimmed) and `admin/mod.rs:718-721` (no `"yes"`).
- **B — `{true,1}`, case-sensitive, untrimmed:** `auth/mod.rs:394-397` (`ALLOW_SIGNUP`, default `"true"`, fail-open by design per `:389-393`); `auth_ui/api/signup.rs:134-135`, `login.rs:75-78`, `refresh.rs:125-128` (`REQUIRE_VERIFICATION`, default `"false"` — the review's "same 3 lines in three handlers", confirmed); `auth_ui/oauth/start.rs:40-43` and `callback.rs:25-28` (`ENABLE_OAUTH`).
- **C — exactly `"true"`:** `auth_ui/pages/login.rs:21-24` (`ALLOW_SIGNUP`), `:53-56` (`ENABLE_OAUTH`), `pages/signup.rs:14-17`; `products/handlers/dispatch.rs:23-25` (`ALLOW_USER_PRODUCTS`); `products/handlers/product.rs:836-844` (`SELLER_MODERATION_REQUIRED`); `products/pages.rs:989-995` (`AUTOMATIC_TAX`); and the render half, `ui/settings_form.rs:117` (`checked[value == "true"]`), which draws an *unchecked* toggle for a value the runtime reads as true.

The review's claim is confirmed with one correction: `ENABLE_OAUTH`'s API side is `auth_ui/oauth/{start,callback}.rs`, not `auth/mod.rs`. Two divergences the review missed are worse, because both halves are in the same block and one of them is the money path: `ALLOW_USER_PRODUCTS` (A at `stripe.rs:989,1318`, C at `dispatch.rs:24`) and `AUTOMATIC_TAX` (A at `stripe.rs:1165,2353`, C at `pages.rs:995`).

Blast radius is bounded on one side: the admin form only ever writes `"true"`/`"false"` (`ui/settings_form.rs:222`, saved verbatim at `:317-365`). Divergent values arrive from env vars, `.env`, seeded workspaces or a direct `config::set`.

Not to be unified with the above (DB value coercion, different problem): `auth/repo/mod.rs:86-93 map_bool`, `tickets/service.rs:653-660 bool_field`, `llm/schema.rs:147-154 parse_enabled_field` (whose unrecognised case is **true**, unlike the other two).

### 1.7 B21, B22, B23

- **B21.** `stripe_catalog_post` (`stripe.rs:1343`, error branch `:1358-1372`) and `stripe_catalog_get` (`:1381`, `:1397-1411`, with a 404→`Ok(None)` special case at `:1394-1396`) classify **every** status ≥ 400 as `FailedPrecondition` (`:1366`, `:1405`) — terminal. `StripeClient::request_json` (`stripe_client.rs:67-128`) classifies `429 || >= 500` as `Internal`/retryable (`:105-113`) and the rest as `FailedPrecondition` (`:114-120`). Confirmed. Five `send_raw` sites in `stripe.rs` bypass `StripeClient` entirely (`:1202`, `:1350`, `:1386`, `:2374`, `:2492`) with five copies of the API-URL literal (`:1198,1594,1990,2370,2484`); the type `StripeClient` is referenced zero times in the file. Two of those sites are *inverted* relative to the catalog pair — `:2391-2411` and `:2493-2498` make **everything** `Internal`, so a deterministic 400 is retried forever. `request_json` already covers POST form bodies (`:82,151-157`), GET (`:70`; used at `stripe_provider.rs:194,326`), idempotency keys (`:73,176-178`) and `Stripe-Account` (`:72,173-175`), which subsumes the three hand-rolled header inserts (`stripe.rs:1193-1195, 2364-2366, 2479-2483`) and `stripe_catalog_headers` (`:1429-1440`). What it does not expose is the raw status/body the current sites log and the 404-means-absent case.
- **B22.** Five parse sites of `IMPRESSPRESS__PRODUCTS__SELLER_APPLICATION_FEE_BPS`: silent-zero at `stripe.rs:996-1005`, `stripe.rs:1325-1334` (both `u16`), `pages.rs:3008-3017`, `pages.rs:3114-3123` (both `u32`); hard error at `stripe_provider.rs:261-277` (`configured_fee`, caller `:349`). `ConfigVar` at `products/mod.rs:244-249`: default `"0"`, `InputType::Number`, not `.optional()`; `settings_form.rs` applies no range check to `Number` (only `Url` gets a pre-save check, `:331-339`).
- **B23.** `IMPRESSPRESS__PRODUCTS__PLATFORM_COUNTRY` `ConfigVar` at `products/mod.rs:220-228`: default `""`, `InputType::Select`, `.optional()`. `stripe.rs:751-760` reads it with default `"US"` **and** falls back to `"US"` on an invalid value; `pages.rs:996-1011` is a hand-copied duplicate of the same ten lines. `stripe_provider.rs:360-371` reads it with default `""` and errors on an invalid non-empty value, omitting `country` from `POST /v1/accounts` when empty (`:387-389`). The consumer is shipping-address collection: `stripe.rs:1166`→`build_offer_checkout_form(:772)`→`push_shipping_address_collection(:655-672)` via `shipping_countries(:623-634)`, and `:2345`→`payment_link_form(:2077)`→`:2116`. Concrete failure: an NZ merchant who enables shipping collection, leaves `allowed_shipping_countries` blank and never sets `PLATFORM_COUNTRY` gets a US-only Checkout.

### 1.8 Where the review is now wrong or already fixed

- **B11 does not reproduce.** `pending_review` and `pending` are values of **two different columns**, and both are advertised correctly. `products.status` is written `"pending_review"` (`handlers/product.rs:1016-1019`), read `"pending_review"` (`handlers/sellers.rs:77`, `pages.rs:737,2127,2132,2137`) and published as the 4-value enum (`contracts.rs:1351-1354`; `products.openapi.json:639-643, 1005-1009, 836-845, 1469-1478`; SDK `extensions.service.ts:305,573`). `products.approval_status` is written `"pending"` two lines later (`handlers/product.rs:1020-1023`), read `"pending"` (`handlers/sellers.rs:76`, `pages.rs:738,2132`) and published as the 5-value enum (`contracts.rs:1380-1383`; `products.openapi.json:525-535`). What is real and worth fixing is adjacent: the two vocabularies are written as bare strings two lines apart, `ApprovalStatus`'s five variants match the *product* column's set exactly yet the enum is bound to `SellerAccount` where only two variants are reachable (`repo/seller_accounts.rs:78-82`), and `ProductStatus` reaches the DB only through serde on the admin PATCH.
- The review's "48 drifted schema literals" and "four-way route declaration" in products are closed by Phase 1; `mod.rs` is 410 lines with no status literals and `routes.rs` derives schemas from contract types.
- `ui::server_error_response` is at `ui/mod.rs:470`, not `:420`, and is no longer caller-free: `files/pages_user/cloudstorage.rs:143` calls it (one of ~25 candidate sites).
- Seven T4 sites the review lists are **already fixed** and must not be re-opened — both legalpages items, `userportal/pages/sessions.rs`, the `reset_password.rs`/`change_password.rs` revocation swallows, `admin/pages/blocks.rs`'s `let _ = set_enabled`, and the products webhook's "not ours vs DB down" for platform subscriptions (1.3). Two more are **deliberate** and must not be "fixed": `auth_ui/api/verify.rs:120-122` and `forgot_password.rs:26` are constant anti-enumeration responses.
- The review's four files `pages_user` sites now all log (`buckets.rs:136,149`, `objects.rs:214`, `cloudstorage.rs:115`); `pages_admin.rs` is eight sites, not four; admin `dashboard.rs`'s five scattered lines are six contiguous ones at `:101-106`; messages' three sites are unchanged and still the only three in the block.
- `default_template_id` did not go away, it moved and kept the defect: `repo/group_templates.rs:33` and `repo/product_templates.rs:12`, both still `-> Option<String>`.
- The review's `role_from_str` at `chat.rs:44-51` is `:38-44`; `ui/mod.rs:420` is `:470`; legalpages' `mod.rs:302-306` is `:311-317` and its `pages.rs:511-519` is `:437-446`.
- `crud.rs`'s "empty-id guard ×6, NotFound/Internal tail ×6, owner preamble ×3" is now one guard, four tails and one `verify_owner`; the duplication moved *out* of `crud.rs` (51 hand-rolled guards elsewhere).
- Products' `handle_webhook` is `stripe.rs:3018-4142`, 1125 lines, not `2983-4104`; the event-type list on the settings page is `pages.rs:2753-2766`, not `2596-2609`; `errors.rs`'s prefix comment is `:126-129`, not `:117-120`; `rate_limit.rs`'s surviving prefix is `:296-312`, not `:363-366`.
- Not verified: whether the committed `products.openapi.json` is byte-current with the tree (no cargo run was permitted); whether `serde_json::Number::to_string()` can emit exponent notation into `Decimal::from_json` (`offer_pricing.rs:107`) — a genuine untested seam; whether the migration *basenames* in `SQLITE_MIGRATIONS` have any consumer at all (`migration_helper.rs:118` says "for the D1 filename" but no reader was found).

## 2. Design

### 2.1 Status, role and kind enums

**2.1.1 One decode door per crate, in `util.rs`.** `enum_column` and `wire_enum` collapse into two functions beside `RecordExt`:

```rust
pub fn enum_column<T: DeserializeOwned>(record: &Record, column: &str) -> Result<T, WaferError>;
pub fn enum_column_or<T: DeserializeOwned>(record: &Record, column: &str, empty: T) -> Result<T, WaferError>;
```

`enum_column` keeps `contracts.rs:1276-1287`'s behaviour (unknown value → `Internal`, naming the row and column). `enum_column_or` is the `wire_enum` case, but takes the fallback as a **typed variant** rather than a string, so the fallback cannot itself be a value the enum does not define. `products/contracts.rs:1276` and `products/repo/offers.rs:34-43` delegate; the content blocks call it directly.

**2.1.2 Where each enum lives.** Each block's own `contracts.rs`, matching the existing convention — no shared enum module. The exception is `EntryRole`, which is owned by `messages/contracts.rs` because messages owns the column; llm converts (2.1.5).

New enums, all `#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, schemars::JsonSchema)] #[serde(rename_all = "snake_case")]`:

| Enum | Home | Variants | Column |
|---|---|---|---|
| `SellerStatus` | `products/contracts.rs` | `NotStarted, Onboarding, Restricted, Active, Suspended` | `seller_accounts.status` |
| `OfferSyncStatus` | products | `NotSynced, Syncing, Synced, Failed` | `offers.sync_status` |
| `ProviderPaymentStatus` | products | `Succeeded, PaymentFailed, Processing, RequiresAction, Canceled` | `purchases.provider_payment_status` (Stripe's vocabulary; owned here because it is our column) |
| `SubscriptionStatus` | products | `Active, Trialing, PastDue, Unpaid, Paused, Canceled, IncompleteExpired` + `#[serde(alias = "cancelled")]` on `Canceled` | `purchases.subscription_status`, `subscriptions.status` |
| `DisputeStatus` | products | the eight from `repo/disputes.rs:29-40` | `disputes.status` |
| `RefundStatus` | products | `Pending, Succeeded, ProviderSucceeded, Failed` | `refunds.status` |
| `EventStatus` | products | `Pending, Processing, Failed, Processed, DeadLetter` | `stripe_events.status` |
| `OperationStatus` | products | `Pending, Processing, Failed, Succeeded, DeadLetter` | `provider_operations.status` |
| `StripeEventType` | products | the 17 at `stripe.rs:3156-4122`, `#[serde(rename_all = "snake_case")]` overridden per variant with `#[serde(rename = "checkout.session.completed")]` etc. | not a column — the webhook `type` field |
| `EntryKind` | `messages/contracts.rs` | `Message, Artifact, Notification, Status` (`messages/pages.rs:342-345`) | `entries.kind` |
| `EntryRole` | messages | `User, Assistant, System`, with `#[serde(alias = "agent")]` on `Assistant` | `entries.role` |
| `DocumentStatus` | `legalpages/contracts.rs` | `Draft, Published, Archived` | `documents.status` |
| `DocumentType` | legalpages | `Terms, Privacy` (the only two any route serves, `legalpages/mod.rs:681-684`) | `documents.doc_type` |
| `ObjectStatus` | `files/contracts.rs` | `Pending, Complete` (`files/repo/objects.rs:40-42`) | objects table |

`ContextType` is deliberately **not** in this table — see decision 5.7.

`ApprovalStatus` splits: `products/contracts.rs:35-42` keeps the five-variant enum and becomes the type of `ProductView.approval_status`; `SellerAccount.approval_status` (`contracts.rs:860`) narrows to a new two-variant `SellerApproval { Approved, Suspended }`, which is all `repo/seller_accounts.rs:78-82` and `stripe_provider.rs:284` can produce. `SellerAccount.status` becomes `SellerStatus`.

**2.1.3 Writes.** Every literal write becomes `serde_json::json!(Enum::Variant)`, the shape `repo/purchases.rs:263` already uses. The three seller ladders (`repo/seller_accounts.rs:187-195, 251-259, 399-407`) collapse into one `fn ladder(suspended: bool, charges_enabled: bool, details_submitted: bool) -> SellerStatus`. `repo/offers.rs`'s `expected_status: &str` parameters (`:566-597`) take `OfferStatus`; `offer_status_label` (`pages.rs:697-703`) and the `match` at `repo/offers.rs:737-741` are deleted in favour of serde. `repo/purchases.rs:1842`'s `"completed"` becomes `OrderStatus::Completed`.

**2.1.4 The webhook match.** `handle_webhook`'s `match event_type` (`stripe.rs:3156`) becomes `match StripeEventType::from_wire(event_type)` with the unknown case returning the existing ignore arm (`:4122`). `pages.rs:2753-2766` renders the list from a `StripeEventType::ALL` const rather than re-spelling it. This is a *display* change to an admin page, so it is a visual-baseline risk (section 3), and it is deliberately **not** the `handle_webhook` split — that is Phase 4's `stripe.rs` work.

**2.1.5 B20.** `role_from_str` (`llm/routes/chat.rs:38-45`) is deleted. `history_to_messages` parses `EntryRole` through serde (so `"agent"` deserialises to `Assistant` via the alias) and maps `EntryRole::{User,Assistant,System}` onto `ChatRole::{User,Assistant,System}` exhaustively; an unparseable role keeps today's behaviour of skipping the entry (`chat.rs:70`) rather than silently becoming a user turn. llm's writes at `chat.rs:127,243` and `streaming.rs:131` pass `EntryRole::User` / `EntryRole::Assistant`. Messages' dropdown (`pages.rs:349`) offers all three variants and submits `assistant`; the renderer's `"agent" | "assistant"` arm (`:38`) becomes a match on `EntryRole`.

**Wire values:** `assistant` wins over `agent`, because `assistant` is what llm has been storing and what `ChatRole` already spells, and `agent` survives as a deserialisation alias so existing rows and existing clients keep working. No stored row is rewritten.

**2.1.6 What moves in the published schema.** Three categories, and each PR states which it is in:

- Replacing a `String` field that already carries `#[schemars(extend("enum" = [...]))]` with the real enum type: the generated schema is the same `{"description", "enum", "type"}` object (compare `approval_status` at `products.openapi.json:525-535` with the real-enum `mode` at `:4846`-adjacent, `{"enum": ["payment","subscription"], "type": "string"}`). **Expected byte-identical**; the PR proves it by regenerating and showing no diff.
- Replacing a bare `String` field with an enum: adds an `enum` array where there was none (`tax_behavior`, `usage_type`, `SellerAccount.status`, every messages field). **Moves the snapshot**, tightening a published contract that was already documented in prose. Each such line is listed in the PR body.
- `Option<Enum>` fields: `recurring_interval` currently renders `{"type": ["string","null"]}`. schemars renders an optional enum differently; the PR must show the produced shape rather than assume it.

**2.1.7 Requests get tighter, and one existing test says how far.** Typing a request-body or query field rejects values the handler previously accepted and stored. For messages that is the point — nothing validates `role` today (`messages/rest.rs:170-171`) — but it is a behaviour change: `POST /b/messages/api/contexts/{id}/entries` with `"role": "bot"` moves from 201 to 400.

`messages/rest.rs:529-558` is the constraint. It exists because a previous commit added a 400-guard on `role` and it was removed; the test now pins that `assistant` and `system` **must be accepted**. `EntryRole` satisfies it as written (both are variants), so the test survives unchanged and is the reason `assistant` — not `agent` — is the canonical spelling. The PR extends it with the `agent`-accepted case rather than replacing it.

`role`'s `#[serde(default)]` (`contracts.rs:63`) currently yields `""`, and `llm/routes/chat.rs:72` drops empty-role entries from model history — so today the messages block's own default makes an entry invisible to the LLM. It becomes `#[serde(default = "EntryRole::user")]`: an omitted `role` stores `"user"` instead of `""`, which is a behaviour change and a strict improvement. Existing `""` rows keep failing to parse; `enum_column_or(record, "role", EntryRole::User)` reads them as user turns, which is what `chat.rs:43` intended and `:72` accidentally prevented.

The list filters (`messages/service.rs:225-243`) parse the same enum and answer 400 on an unknown filter value rather than silently returning an empty page.

### 2.2 The error model

**2.2.1 `From<ErrorCode> for OutputStream`.** The one `From` impl orphan rules allow:

```rust
impl From<ErrorCode> for wafer_run::OutputStream { ... }   // in blocks/errors.rs
```

It is `error_response(code, code.default_message())` — a message table beside `as_str`. `error_response(code, message)` stays for the sites that need a specific message. This lets `?` carry an `ErrorCode` out of a handler and lets a match arm read `ErrorCode::NotFound.into()`.

**2.2.2 `crud::db_error`.** One function replacing the four copies inside `crud.rs` and the 58 outside it:

```rust
pub fn db_error(e: WaferError, not_found: &str, context: &str) -> OutputStream
```

`NotFound` → `err_not_found(not_found)`; **`PermissionDenied` → `err_forbidden`** (the behaviour fix: a WRAP denial stops being a 500); `ResourceExhausted` → the 429 the wafer mapping already gives; everything else → `err_internal(context, e)`. `crud.rs:119-125, 161-167, 181-187, 232-235` become one call each; `list_page:106-108` and `create_record:143-148` call it with no 404 case through a `db_error_internal` sibling.

**2.2.3 `http::require_row`.** For the 38 repo functions now returning `Result<Option<_>>`:

```rust
pub fn require_row<T>(row: Option<T>, not_found: &str) -> Result<T, OutputStream>
```

so a handler reads `let user = require_row(repo::users::find_by_id(ctx, id).await.map_err(db_error_for)?, "User")?;`. It lives in `http.rs` beside `redirect`, not in `crud.rs`, because it has nothing to do with the generic-table helpers.

**2.2.4 `RepoError` folds into `WaferError`.** `auth::repo::RepoError` (`auth/repo/mod.rs:36-44`) discards the wafer code, which is exactly what 2.2.2 is trying to preserve. The 15 auth repo functions return `Result<_, WaferError>` like the products and dev repos, and `RepoError` is deleted. This is the single largest mechanical part of the error PRs and is why auth gets its own PR.

**2.2.5 `errors.rs`.** `status_code()` and its test (`errors.rs:80-111, 178-215`) are deleted: zero callers, and its one substantive claim (413 for quota) contradicts the 429 that actually ships. `ErrorCode`, `as_str`, `Display`, `error_response` and the new `From` stay; `impresspress_error_code_to_wafer` becomes `pub(crate)` still but gains a doc line naming `http_codec::error_code_to_http_status` as the authority. See decision 5.3 for the alternative.

**2.2.6 `rate_limit::rate_limited_response`.** `rate_limit.rs:296-312` drops the `"[{}] "` prefix and attaches the detail code through `WaferError::with_detail_code(ErrorCode::RateLimitExceeded.as_str())`, keeping its hand-built `Retry-After` meta. That is a wire change on a 429 body: `message` loses the bracket prefix and the response gains a `code` field the SDK already reads (`http-client.ts:167`). No test pins the current body.

**2.2.7 The 51 empty-id guards.** Every `if id.is_empty() { err_bad_request("Missing X ID") }` becomes `crud::path_id(msg, "X")?`. `crud.rs:44-47` argues the guard is unreachable through the route table; the PR keeps `path_id` (it is the documented door) and deletes the copies.

### 2.3 Money

**Ruling 2 splits this item in two.** The correctness half — one decimal parser and one published source of truth for the duplicated columns — is done in this phase, as PR 7 (2.3.6, 2.3.7). The column rename and the wire rename are **deliberately deferred** and are not in this phase; 2.3.1–2.3.5 are kept below, unchanged, as the plan that a later release would execute.

The reason for the split: the stored values are already correct minor units (`repo/purchases.rs:265-272` writes `snapshot.amounts.total_minor` into `total_cents`), so `_cents` is a misleading *name* on correct data. The two decimal parsers disagreeing (1.5, last bullet) is a live correctness bug on the money path. A live-data migration plus a published wire rename, run unattended, is the worst risk-to-value ratio in the programme; the parser unification and the duplicate-column de-publication carry the same correctness benefit for a tenth of the diff and no migration.

#### 2.3.a Deliberately deferred: the `_cents` → `_minor` column and wire rename

Not in this phase. What it would take, kept so it can be scheduled as its own release: 2.3.1–2.3.5 below, in full — an additive migration `021` (Stage A), a dual-write release (Stage B) that also moves every published wire name, the SDK and both CI `information_schema` assertions, and a later `022` that drops the old columns (Stage C). The blocking facts are that `apply_if_blessed` re-runs the whole concatenated corpus on any hash change and tolerates only a duplicate `ADD COLUMN` (`migration_helper.rs:193-212`), that no `RENAME COLUMN`/`DROP COLUMN` precedent exists anywhere in the repo, and that the seven names are published in `products.openapi.json`, `dev.tools.json`, the SDK and two CI workflows. Nothing in this phase makes that work harder: PR 7 leaves every column name exactly as it is.

**2.3.1 What is renamed.** `total_cents → total_minor`, `subtotal_cents → subtotal_minor`, `discount_cents → discount_minor`, `tax_cents → tax_minor`, `shipping_cents → shipping_minor`, `platform_fee_cents → platform_fee_minor`, `refunded_total_cents → refunded_total_minor`. `amount_cents` and `user_id` are **dropped**, not renamed: `amount_cents` has one reader publishing a field documented as legacy (`contracts.rs:2165, 2630`), and `user_id` is a mirror of `buyer_user_id` with a divergent read set (1.5).

Note that six of the seven new names already exist as `_minor` columns on **other** tables (`line_items`, `refunds`), which is the point — after this, one name means one thing.

**2.3.2 Stage A — migration 021, additive only.** `021_purchase_amount_columns.{sqlite,postgres}.sql`:

```sql
ALTER TABLE impresspress__products__purchases ADD COLUMN total_minor INTEGER NOT NULL DEFAULT 0;
-- … six more ADD COLUMNs …
UPDATE impresspress__products__purchases SET total_minor = total_cents, … WHERE total_minor = 0;
```

Every statement is re-run-safe by the two mechanisms the repo already relies on: `ADD COLUMN` duplicates are tolerated (`migration_helper.rs:193-212`) and the backfill `UPDATE` is idempotent because Stage B writes both columns, so re-running it copies a value onto itself. The postgres file uses `ADD COLUMN IF NOT EXISTS` as `005_commerce_v2.postgres.sql:171-175` does. **No `RENAME COLUMN` and no `DROP COLUMN`** — neither is tolerated on re-run and neither has a precedent in this repo (1.5).

The `WHERE total_minor = 0` guard is what makes the backfill safe on re-run *and* on a fresh database where the ADD COLUMN default already applies; a legitimately-zero order is copied 0 onto 0.

**2.3.3 Stage B — dual write, single read.** `repo/purchases.rs`'s two writers (`:265-272`, `:838-845`) write **both** spellings of each column. Every reader moves to `_minor`. This is the release that can be rolled back: an old binary against a new database still finds `_cents` populated.

**2.3.4 Stage C — contract, next release, not in this phase.** A `022` that drops the seven `_cents` columns plus `amount_cents` and `user_id`, and a PR that removes the dual write. It is out of scope here because a `DROP COLUMN` is not re-run-safe under `apply_if_blessed` either, and designing that (a `DROP … IF EXISTS` on postgres, a guarded sqlite path, or accepting a one-way migration behind `IMPRESSPRESS_RUN_MIGRATIONS`) is its own piece of work. Recorded in `RELEASE.md`'s Upgrade Notes, which 020 already established as the place for this.

**2.3.5 The wire.** Because every view is hand-projected (`contracts.rs:2619-2636`), the wire names are a *separate* decision from the column names. Recommendation (5.1): rename the wire fields in the same PR as Stage B, because leaving `total_cents` on the wire while the column is `total_minor` is precisely the "translation between representations" `CLAUDE.md` forbids. That makes Stage B a breaking API change and it must move together:

- `products.openapi.json`: seven property keys per order-shaped schema (`:5561-5783` and the three other order projections), plus the removal of `amount_cents` and `user_id`.
- `dev.tools.json`: the same schemas are embedded in the WebMCP tool manifest (`dev.tools.json:193-1168` carries the products contract schemas), regenerated with `UPDATE_DEV_TOOLS_SNAPSHOT=1` under `--features block-dev`.
- `pipeline.rs:1547-1550`: `refunded_total_cents` → `refunded_total_minor`.
- SDK `extensions.service.ts:734, 745-752, 792-797, 854-860`.
- `ci.yml:1251` and `ci-main.yml:871`: `shipping_cents` → `shipping_minor`.
- `products/pages.rs:2440, 3203, 3309, 3313, 3377, 3385-3389, 3768`.

#### 2.3.b In this phase (PR 7)

**2.3.6 `money::Decimal`.** The dead `money::parse_amount_minor` (`money.rs:41-94`) and the live `offer_pricing::Decimal` (`:58-111`) become one type in `money.rs`: `Decimal` moves there, keeps its scale-9 cap and its exact arithmetic, and gains `to_minor(currency) -> Result<i64, String>` built from `parse_amount_minor`'s exponent logic (`money.rs:63-93`) — so the currency exponent finally reaches the pricing path. `parse_amount_minor` becomes `Decimal::parse(s)?.to_minor(currency)`. The sign difference is resolved explicitly: `Decimal::parse` keeps accepting negatives (refund and adjustment paths need them) and `to_minor` rejects them unless a `to_signed_minor` is asked for. `offer_pricing.rs` imports the type instead of defining it.

**2.3.7 One published source of truth for the duplicated columns.** The two duplicate pairs are a *publication* problem before they are a *storage* problem, and the publication half needs no migration because every view is hand-projected (`contracts.rs:2619-2636`).

- `PurchaseView.amount_cents` (`contracts.rs:2165`, fed by `:2630`) is removed from the view. It is documented as legacy, it has exactly one reader, and it publishes the `amount_cents` column while `total_cents` beside it publishes the same value from a different column — so a pre-005 row where the two disagree ships a wrong number under a field whose own doc string says to prefer the other one. `total_cents` stays the single published amount.
- `PurchaseView.user_id` (`contracts.rs:2131-2133`) is removed from the view; `buyer_user_id` is the published identity, which is what per-row ownership already checks (`purchase.rs:242-246`). The published doc string claims `user_id` is empty for guest orders while the writer mirrors `""` into both, so the field is documented wrongly as well as duplicated.
- The columns themselves stay, both still written by the one writer pair (`repo/purchases.rs:241-248, 265-272, 838-845`), so nothing is migrated and an older binary reading the database still finds what it expects. The internal read split (`user_id` for order lists, `buyer_user_id` for the Billing-Portal lookup and ownership) is unified onto `buyer_user_id` in the same PR, which is what removes the drift risk; the `user_id` fallback at `purchase.rs:242-246`, `repo/purchases.rs:513-517` and `pages.rs:3276-3280` is kept, since it is the compatibility path for pre-005 rows.

**This moves `products.openapi.json` and `dev.tools.json`** (two removed properties per order-shaped schema) and the SDK's three order interfaces (`extensions.service.ts:734, 745-752, 792-797, 854-860`), so PR 7 carries a reviewed diff for all three. It does **not** touch `pipeline.rs:1547-1550`, `ci.yml:1251` or `ci-main.yml:871`, which assert on `refunded_total_cents` and `shipping_cents` — columns this phase does not rename.

### 2.4 One boolean truth table

**2.4.1 `config_vars::get_bool`.** In `config_vars.rs` beside `shared_var`/`var_in`, not in `wafer_core::clients::config` (which is upstream — decision 5.4):

```rust
pub async fn get_bool(ctx: &dyn Context, key: &str, default: bool) -> bool
```

Reads through `config::get_default(ctx, key, if default {"true"} else {"false"})` and applies **table A**: trim, ASCII-lowercase, `matches!(v, "1" | "true" | "yes" | "on")`. Table A is chosen over table B/C because it is the superset — every value that is true under B or C is true under A, so no deployment that works today stops working. It is also what `products/stripe.rs:558-563` and `tickets/config.rs:201-206` already implement.

**2.4.2 Call sites.** All of table A (`stripe.rs:558-563` deleted, `tickets/config.rs:201-206` deleted, `tickets/service.rs:620-631` deleted), all of table B (`auth/mod.rs:394-397`, `auth_ui/api/{signup,login,refresh}.rs`, `auth_ui/oauth/{start,callback}.rs`) and all of table C (`auth_ui/pages/{login,signup}.rs`, `products/handlers/dispatch.rs:23-25`, `products/handlers/product.rs:836-844`, `products/pages.rs:989-995`) call `get_bool`. The `auth_ui/pages/*` sites read through `ctx.config_get` rather than the config client; they move to `get_bool` too, which is also what makes `ALLOW_SIGNUP=1` stop hiding the signup link.

**2.4.3 The render half.** `ui/settings_form.rs:117` (`checked[value == "true"]`) uses the same predicate, so a toggle set to `1` from the environment draws as on.

**2.4.4 Form and DB coercion stay separate.** `tickets/public.rs:624-631` and `vector/contracts.rs:206-207` read *form* fields and `admin/mod.rs:718-721` reads a form field; they get a sibling `form_bool` with the same table (an HTML checkbox posts `on`). `auth/repo/mod.rs:86-93`, `tickets/service.rs:653-660` and `llm/schema.rs:147-154` decode *database* values, where `Bool` and `Number` are the real cases; they are unified separately onto `RecordExt::bool_field`, which `auth/repo/users.rs:148-151` already documents as the round-trip for both backends. `llm/schema.rs:147-154`'s fail-open default (unrecognised → `true`) is preserved deliberately or changed deliberately, not by accident — it decides whether a provider is enabled.

### 2.5 B21, B22, B23

- **B21.** `stripe_catalog_post`/`stripe_catalog_get` (`stripe.rs:1343-1411`) route through `StripeClient::request_json`. `request_json` gains one thing it lacks: the caller needs the status for the 404-means-absent case, so it returns the decoded value plus the status, or a `StripeError { code, status, body }` — the smallest change is to make the `FailedPrecondition` error carry `resp.status` in its `meta` so `stripe_catalog_get` can read 404 from it. The other three raw sites (`:1202`, `:2374`, `:2492`) move in the same PR because they share the header building and the API-URL literal; their inverted classification (`Internal` for everything, `:2391-2411`, `:2493-2498`) is fixed by the same routing. `stripe_catalog_headers` (`:1429-1440`), `stripe_request_headers` (`:4301-4307`) and the five API-URL literals are deleted. Retry behaviour is otherwise unchanged: this PR does not add a retry loop, it only stops classifying a 503 as terminal.
- **B22.** One function in `products/mod.rs` or a new `products/config.rs`: `pub async fn seller_fee_bps(ctx: &dyn Context) -> Result<u16, WaferError>`, the body of `stripe_provider.rs:261-277` (hard error, since a mis-set fee silently becoming 0 is money lost). All five sites call it. The two `pages.rs` sites currently swallow into 0 for *display*; they render the error state rather than a false 0. `u32` is dropped — `u16` covers 0..=10000 and the cap is checked in one place.
- **B23.** `pub async fn platform_country(ctx: &dyn Context) -> Result<Option<CountryCode>, WaferError>` in the same module: `""` → `Ok(None)`, a valid two-letter code → `Ok(Some(..))` uppercased, anything else → the `FailedPrecondition` `stripe_provider.rs:364-371` already returns. `stripe.rs:751-760` and `pages.rs:996-1011` are deleted. `shipping_countries` (`stripe.rs:623-634`) takes `Option<CountryCode>`; with `None` and an empty `allowed_shipping_countries` it emits **no** `shipping_address_collection[allowed_countries]` key at all, which is Stripe's "all countries" behaviour — not a silent US. That is a behaviour change for any deployment relying on the `"US"` default; it is called out in the PR and in `RELEASE.md`.

### 2.6 The remaining swallowing sites (T4)

Three PRs, one per group from 1.3, each closing a set that shares one fix shape.

**2.6.1 Page renderers (group 1).** Every SSR renderer that today produces an empty state takes a `Result` and, on `Err`, returns `ui::server_error_response(msg)` (`ui/mod.rs:470`) — the pattern `files/pages_user/cloudstorage.rs:139-144` already models and which one of ~25 sites uses. The rule is stated once so it does not have to be argued 25 times: **a page renders a fallback only for an empty result, never for an error.** Two sites need a different answer rather than the rule: `admin/pages/dashboard.rs:101-106` aggregates six independent reads through `futures::join!`, so it renders the tiles it has and an inline "could not load" marker per failed tile rather than failing the whole page (the dashboard is the page an operator opens *during* an outage); and `files/storage/admin.rs:14, 18` are a JSON API, so they propagate through `db_error` rather than rendering anything.

**2.6.2 llm persistence (group 2).** `messages_create` (`llm/mod.rs:311-343`) returns `Result<serde_json::Value, WaferError>` instead of `Option`, propagating both swallows at `:338-342`. `chat.rs:127`'s `let _ =` on the user turn becomes `?`: **a turn that was not stored must not be followed by a model call**, because the next request rebuilds history from the store and the user's message is simply gone. `streaming.rs:131`'s assistant write and `chat.rs:243-252` propagate too — a stream that has already emitted content ends with an SSE `error` event before `[DONE]` rather than a silent success with `message_id: ""`. `messages_list` (`llm/mod.rs:351-363`) returns `Result`, so `chat.rs:130` refuses rather than sending a context-free prompt to a paid provider.

**2.6.3 Products honesty (group 3).** Every `Option`/`bool` return where the caller cannot distinguish "not ours" from "DB down" becomes `Result<Option<_>>`/`Result<bool>` and every caller three-way branches — the shape `platform_subscription_exists` (`repo/subscriptions.rs:185-193`) and its four webhook callers already establish. That covers `find_user_by_stripe_sub` (`:467-486`), `active_plan_exists` (`:497`), `line_item_exists_for_product` (`repo/purchases.rs:1854-1884`), `user_owns_product` (`stripe.rs:4311-4326`) and both `default_template_id` twins (`repo/group_templates.rs:33`, `repo/product_templates.rs:12`). The nine compensating `let _ =` (`repo/purchases.rs:369`; `stripe.rs:1182, 1213-1218, 1230-1235, 1241-1246, 1269-1274, 2385, 2401, 2427`) keep proceeding with the original error — that is correct for a compensation — but log at `error!` with the order/link id, so a stuck row is findable. Where the outer function is already returning an error, the compensation failure is attached to it rather than dropped.

**2.6.4 Auth (group 4), and what is deliberately left.** The eight collapsing lookups (`me.rs:40-42`, `api_keys.rs:180-182, 203-205`, `change_password.rs:41-44`, `refresh.rs:118`, `verify.rs:58-63`, `reset_password.rs:38-39`, `userportal/pages/admin_buttons.rs:252-254`) separate `Ok(None)` from `Err`, adopting the paragraph `login.rs` and `signup.rs` already carry. `verify.rs:120-122` and `forgot_password.rs:26` are **not** changed: they are deliberate constant responses against account enumeration (rationale at `verify.rs:113-118`). The PR adds a comment saying so at both, so the next sweep does not re-open them.

**2.6.5 Not here.** The three runtime sites (1.3, last paragraph) are Phase 4, and one of them — `impresspress-cloudflare/src/config_service.rs:26-28` — cannot be fixed in this repo at all, because `ConfigService::set` returns `()` upstream.

## 3. Testing

### 3.1 Existing gates that apply

- `tests/openapi_snapshot.rs` and `tests/endpoint_surface.rs` (regen `UPDATE_OPENAPI_SNAPSHOTS=1`). Both must be byte-identical except where section 4 says otherwise. Note `SNAPSHOTTED_BLOCKS` (`openapi_snapshot.rs:28-38`) and `test_support::real_block_infos()` must both list a block or the gate is vacuous — the trap Phase 2 PR 5 documented.
- **A third snapshot gate the earlier specs did not name:** `tests/dev_tools_manifest.rs:126-156` pins `tests/snapshots/dev.tools.json`, which embeds the products contract schemas (`dev.tools.json:193-1168`). It only runs under `--features block-dev` and regenerates with `UPDATE_DEV_TOOLS_SNAPSHOT=1`. Every products contract change moves it.
- `tests/repo_door.rs` — the enum work adds no table names, so it should stay green; the money PR touches `repo/purchases.rs` column strings, which the door already covers.
- `scripts/audit-wrap-grants.sh` (`ci.yml:82-83`) — unchanged by this phase; no `db::*` call moves between modules.
- Per-block migration tests: `blocks/products/migrations/mod.rs:169-380` already drives the real harness (`apply_ddl_via_service`) across a pre-004 schema, a 004 ALTER and the full corpus. No migration is added in this phase (ruling 2), so this gate is untouched.
- The postgres lane (`ci.yml:236-255`, `cargo test -p impresspress-core --features postgres`) and the `information_schema` assertions (`ci.yml:1240-1252`) — unchanged in this phase, since no column is renamed. The deferred rename (2.3.a) owns them.
- Visual baselines (`regen-visual-baselines.yml`) — 2.1.4's event-type list and 2.5's fee/country error states change admin page HTML.
- The SDK job (`ci.yml:1296-1340`) — the money PR and any request-schema tightening change `packages/impresspress-js`.

### 3.2 New tests, test-first

- **Enums (per block).** For each new enum: a round-trip test that `serde_json::to_value(Variant)` equals the literal the migrations/live rows hold; `enum_column` on a record holding a value outside the set returns `Internal` naming the row; `enum_column_or` on an empty column returns the typed fallback. For `SubscriptionStatus`, a test that a row holding `"cancelled"` parses to `Canceled` (fails today only in the sense that `subscription_status_rank` is the only thing that knows). For `SellerStatus`, one test that the single `ladder()` reproduces all three former copies' outputs across the eight input combinations.
- **B20.** `history_to_messages` on entries with roles `user`, `assistant`, `agent`, `system` produces `User, Assistant, Assistant, System` — **fails today** (`agent` → `User`). A round-trip test that an entry created through `POST /b/messages/api/contexts/{id}/entries` with `"role":"agent"` is replayed to the model as an assistant turn.
- **Error model.** `db_error` on each of `NotFound`, `PermissionDenied`, `ResourceExhausted`, `Internal` produces 404/403/429/500 through `test_support::output_http_status`. An end-to-end test on a `TestContext::with_wrap` that denies a read: the handler answers **403**, not 500 — fails today at all 62 sites. `require_row(None, "User")` is a 404 with that label. A grep-style test in the spirit of `tests/repo_door.rs` asserting no file under `blocks/` outside `crud.rs` matches the NotFound/`err_internal` pair — that is what keeps the 62 from coming back.
- **`errors.rs`.** Deleting `status_code()` deletes `test_error_code_status_codes` (`errors.rs:178-215`); the replacement asserts that `error_response(ErrorCode::QuotaExceeded, ..)` resolves to **429** through `http_codec::resolve_error_status`, pinning the real mapping instead of the dead one. A test that `rate_limited_response` carries `detail_code() == Some("rate_limit_exceeded")` and a message with no `[` prefix — fails today.
- **Money (PR 7).** A `Decimal::to_minor` table across `USD` (2), `JPY` (0) and `KWD` (3), including the cases `money.rs:144-152` already pins, plus the cases where the two parsers disagree today: scientific notation, a negative, and a fractional digit beyond the currency's exponent — each asserted once against the one surviving parser. A test that `PurchaseView` no longer publishes `amount_cents` or `user_id`, and one that an order whose `user_id` and `buyer_user_id` disagree is listed and access-checked under the same identity — fails today.
- **B21.** `stripe_catalog_post` against a fake transport returning 503 yields a retryable classification (`Internal`), not `FailedPrecondition` — fails today. 400 stays terminal. `stripe_catalog_get` against 404 still yields `Ok(None)`.
- **B22.** `seller_fee_bps` on `"abc"`, `"20000"`, `""` each returns `Err`; on `"250"` returns 250. A checkout with a garbage fee refuses instead of charging 0 — fails today.
- **B23.** `platform_country` on `""` returns `Ok(None)`; on `"nz"` returns `NZ`; on `"NZL"` returns `Err`. A checkout with `PLATFORM_COUNTRY` unset, shipping collection on and no allowed list emits no `allowed_countries` key — fails today (emits `US`).
- **`get_bool`.** One table test over `"1" "true" "TRUE" " on " "yes" "0" "false" "" "bogus"`, plus a test that `ALLOW_USER_PRODUCTS=1` opens the route guard and the checkout path together — fails today.
- **T4.** Each group gets its fault-injection test on the existing helpers (`TestContext::break_reads`/`break_list_reads`, `FailingDbOpContext` at `test_support.rs:1336-1394`): every converted page renders the error page instead of an empty state; `chat.rs`'s user-turn write failing means no provider call is made (assert on a recording provider that it received nothing) — fails today; `messages_list` failing means the chat request 500s instead of prompting with no history — fails today; `user_owns_product` on a failing read produces a 500, not "you must own the required product" — fails today. The two deliberate anti-enumeration sites get a test pinning that they still answer identically for a known and an unknown email, so a later sweep cannot quietly change them.

### 3.3 PRs that move a published schema

Verified against the committed snapshots, which is a narrower list than expected:

- **PR 3 (content-block enums)** moves `messages.openapi.json` only — `kind` and `role` gain an `enum` array where the snapshot today has none anywhere. It moves **neither** `files.openapi.json` (object status is not published; the file has no `status` at all) **nor** `legalpages.openapi.json` (1.6 KB, `PATCH` only; `doc_type` never appears) **nor** `llm.openapi.json` (no `role` field exists in it), so B20's llm half has no published-contract cost.
- **PR 4 (products enums)** moves `products.openapi.json` and `dev.tools.json`. The `String`+`schemars(extend)` fields are expected byte-identical (2.1.6); the genuinely new `enum` arrays are `SellerAccount.status`, `sync_status`, `provider_payment_status`, `subscription_status`, `refunds.status`, the two operation-status fields and `ProviderOperationSummary.status`.
- **PR 7 (money correctness)** moves `products.openapi.json` and `dev.tools.json` by *removing* two properties per order-shaped schema (`amount_cents`, `user_id`), and moves the SDK's three order interfaces. It does not touch `pipeline.rs`, `ci.yml` or `ci-main.yml`.

Everything else is expected byte-identical, and each PR body says so explicitly rather than leaving it implied.

## 4. Sequencing

**Ten PRs** against `main` (ruling 8), each green and mergeable alone. 1 precedes 2 and 8–10 (they use `db_error`); 4 precedes 7 purely to avoid conflicts in the products contracts. Nothing else is ordered.

1. **Error helpers and `errors.rs` cleanup.** `crud::db_error`, `http::require_row`, `From<ErrorCode> for OutputStream`, delete `status_code()`, fix `rate_limited_response`, convert `crud.rs`'s four tails and the empty-id guards. Files: `blocks/errors.rs`, `blocks/crud.rs`, `blocks/rate_limit.rs`, `http.rs`, and the guard sites across products/admin/llm/files/auth_ui/tickets/legalpages. Core only. Closes the T4 error-type item and the `[code]` prefix. *Adds the anti-regression grep test.* The remaining hand-mapped sites are converted by PR 2 (auth/admin) and by the block PRs that already open those files; the PR body carries the explicit remaining list so none is lost.
2. **`RepoError` folded into `WaferError`; auth and admin call sites onto `db_error`.** Files: `blocks/auth/repo/*`, `blocks/auth/*`, `blocks/auth_ui/*`, `blocks/userportal/*`, `blocks/admin/{ops,iam,mod}.rs`. Touches the **CLI** only through `tests/` if auth fixtures move. After 1.
3. **Content-block enums (messages, files, legalpages) and B20.** New enums in the three `contracts.rs` files, `EntryRole` consumed by llm, the shared `enum_column` in `util.rs`. Files: `blocks/messages/{contracts,service,rest,pages}.rs`, `blocks/legalpages/*`, `blocks/files/{contracts,repo,pages_*}.rs`, `blocks/llm/routes/{chat,streaming}.rs`, `util.rs`. **Moves `messages.openapi.json` only** (3.3). Touches the **SDK** if it types these bodies — verify before, per the consumer checklist.
4. **Products enums.** `SellerStatus` (+ the one ladder), `OfferStatus` writes, `OfferSyncStatus`, `ProviderPaymentStatus`, `SubscriptionStatus` (+ the `cancelled` alias), `DisputeStatus`, `RefundStatus`, `EventStatus`, `OperationStatus`, `ApprovalStatus`/`SellerApproval` split, `ProductStatus` on the two views. Files: `blocks/products/{contracts,pages}.rs`, `repo/{seller_accounts,offers,purchases,subscriptions,disputes,refunds,provider_operations}.rs`, `handlers/{provider,sellers,product}.rs`, `stripe.rs`, `stripe_provider.rs`. **Moves `products.openapi.json` and `dev.tools.json`.** Touches the **SDK** (`extensions.service.ts:305,321,573,621-626,652-657,962-963` — note `:962-963` already erases `ApprovalStatus`) and the **visual baselines**.
5. **`StripeEventType` and the settings-page list.** Split from 4 because it changes an admin page's rendered HTML and therefore needs the baseline regen on its own. Files: `blocks/products/{stripe,pages}.rs`.
6. **B21, B22, B23 and `get_bool`.** `StripeClient` routing for the five raw sites, `seller_fee_bps`, `platform_country`, `config_vars::get_bool` and all its call sites. Files: `blocks/products/{stripe,stripe_client,stripe_provider,pages,mod}.rs`, `handlers/{dispatch,product}.rs`, `config_vars.rs`, `blocks/auth/mod.rs`, `blocks/auth_ui/{api,oauth,pages}/*`, `blocks/tickets/{config,service,public}.rs`, `blocks/vector/contracts.rs`, `blocks/admin/mod.rs`, `ui/settings_form.rs`. Core only, but **behaviour-changing for deployments relying on the `US` default** — needs a `RELEASE.md` note.
7. **Money correctness: `money::Decimal` unification and one published amount.** `Decimal` moves to `money.rs`, gains `to_minor(currency)`, `parse_amount_minor` is re-expressed on it and `offer_pricing.rs` imports the type instead of defining a second one (2.3.6); `PurchaseView` stops publishing `amount_cents` and `user_id` and the internal reads unify onto `buyer_user_id` (2.3.7). Files: `blocks/products/{money,offer_pricing,contracts,pages}.rs`, `repo/purchases.rs`, `purchase.rs`, plus `packages/impresspress-js/src/services/extensions.service.ts`. **Moves `products.openapi.json` and `dev.tools.json`** (two removed properties per order-shaped schema). Touches the **SDK**. No migration, no column renamed. After 4.

8. **T4 group 1 — page renderers.** `files/pages_user/*.rs`, `files/pages_admin.rs`, `files/storage/admin.rs`, `messages/pages.rs`, `admin/pages/{dashboard,network,storage,blocks,permissions}.rs`, `llm/pages.rs`. Core only, but every one of these is a rendered page, so it needs the **visual-baseline** run. After 1.
9. **T4 group 2 — llm persistence, and group 3 — products honesty.** `llm/{mod,pages}.rs`, `llm/routes/{chat,streaming}.rs`; `products/repo/{purchases,subscriptions,group_templates,product_templates}.rs`, `products/stripe.rs`, `products/handlers/{group,product}.rs`. Core only. After 1; after 4 and 6 if they have landed, purely to avoid conflicts in `stripe.rs`.
10. **T4 group 4 — auth.** `auth_ui/api/{me,api_keys,change_password,refresh,verify,reset_password}.rs`, `userportal/pages/admin_buttons.rs`, plus the two comments marking the anti-enumeration sites as deliberate. Core only. After 2 (same files).

PRs 8–10 stay separate from the enum PRs deliberately (ruling 8): the enum PRs already move snapshots, and error-swallowing is the theme with the most fail-open behaviour left in the tree — it is why several Phase 0 bugs existed, so it gets reviewed on its own. PRs 1–7 are the phase's spine.

## 5. Decisions

Ruled on 2026-09-06; see "Decisions taken" at the top for what was decided. This section is the reasoning that produced those rulings and is kept unedited as the record. Where a recommendation below differs from a ruling — 5.1 (money) is the only one — the ruling governs.

**5.1 Is the money rename worth doing in this phase?** *Recommend yes, as Stage A + B only (2.3.2–2.3.5), with Stage C deferred.* Evidence for: the name is provably wrong (`money.rs:24-33` supports 0- and 3-decimal currencies, and the published description of `total_cents` already says "in minor units", `products.openapi.json:5783`); six of the seven target names already exist on sibling tables, so the codebase currently uses two names for one concept *within one block*; and the two duplicate pairs (`amount_cents`, `user_id`) are a live correctness risk, not just a naming one, because their read sets differ (1.5). Evidence against: it is the only item in the phase that touches live rows, it breaks a published API and the SDK, and it costs a migration in a runner with no rename precedent. The staging removes the data risk entirely — Stage A only adds columns and copies values, both re-run-safe — and defers the only irreversible step. **If the ruling is "not now", the fallback is to do 2.3.6 (`Decimal`) and drop `amount_cents`/`user_id` from the *views* alone, leaving the columns; that closes the correctness risk for a tenth of the diff.**

**5.2 Does B11's fix change the wire or the code?** *Recommend neither: B11 is closed as not reproducible.* Evidence: `pending_review` and `pending` are values of two different columns, each written, read and published consistently (1.8, with eight cited sites). The PR that closes it adds a regression test asserting the invariant the code maintains by hand — `status == pending_review` ⟺ `approval_status == pending` at `handlers/product.rs:1016-1023` — and binds `ProductView.approval_status` to `ApprovalStatus` so the two vocabularies stop being adjacent bare strings. That is a code change with an expected byte-identical schema (2.1.6, first category). No wire value changes.

**5.3 Should `errors.rs` become the single error constructor, or be deleted?** *Recommend: keep it, shrink it, and do not migrate the 847 `err_*` sites to it.* Evidence: `errors::ErrorCode` is the only thing that produces the `error.code` detail meta the SDK reads (`http-client.ts:167`, `error.ts:22`); `err_*` produce none. Deleting `errors.rs` would drop a machine-readable code from 36 auth responses that clients may switch on. Migrating the other way — 847 sites onto `ErrorCode` — is a phase of its own and would need a detail code invented for each. So: `errors.rs` keeps `ErrorCode`, `as_str`, `Display`, `error_response` and gains `From`; it loses `status_code()`; `err_*` stay the default for everything that does not need a fine-grained code. The one rule added is that no handler builds a `WaferError` by hand (the `rate_limit.rs:304` shape) — that is what 2.2.6 fixes. **The alternative worth naming:** give `err_*` an optional detail code upstream and delete `errors.rs` entirely — that is a wafer-run change and belongs in Phase 4's upstream list.

**5.4 `config_client::get_bool` or `config_vars::get_bool`?** *Recommend `config_vars::get_bool` (2.4.1).* `config_client` is an alias for `wafer_core::clients::config`, so the review's phrasing implies an upstream change; the brief puts upstream work in Phase 4. A local helper reads the same `get_default` and costs nothing. **Recorded for Phase 4's upstream list:** `config::get_bool` in `wafer-core`, at which point the local one becomes a re-export.

**5.5 Which truth table wins?** *Recommend table A (`{1,true,yes,on}`, trimmed, case-insensitive).* It is the superset, so no working deployment breaks; it is already what the two most-used helpers implement; and the admin UI only ever writes `true`/`false` so the widened set is only reachable from env/`.env`, where `1` is the conventional spelling. The alternative — table C, exactly `"true"` — would silently *disable* features for anyone using `=1` today, which is the same class of bug as the one being fixed.

**5.6 Does `StripeClient` need to expose the status code?** *Recommend yes, as `meta` on the error rather than a new return type.* `stripe_catalog_get` needs 404-means-absent (`stripe.rs:1394-1396`) and three sites log the raw status (`:1223-1229`, `:2376-2382`). Attaching `resp.status` to the `WaferError` meta keeps `request_json`'s signature and is the smallest change; the alternative (a `StripeError` struct) is cleaner but is really part of Phase 4's `stripe.rs` split.

**5.7 The `ContextType` variant set.** *Recommend leaving `context_type` a `String`.* `messages/contracts.rs:32` documents "conversation, task, notification, **etc.**" — unlike `role` and `kind`, there is no closed set, and the block's own code only ever tests one value (`pages.rs:184, 263`, both `"conversation"`). Typing a field whose documented set ends in "etc." is how a 400 arrives for a value that was always legal. The alternative, an enum with an `Other(String)` escape, buys nothing: it validates nothing and still moves the snapshot. If someone can name the closed set, this flips.

**5.8 How far to tighten the messages request bodies.** *Recommend: type `role` and `kind`, keep the removed 400-guard removed in spirit.* `messages/rest.rs:529-558` exists because a `role` guard was added once and reverted; the enum satisfies that test as written because `assistant` and `system` are variants. But it does mean `POST .../entries` with an unrecognised `role` moves from 201 to 400, which is a wire behaviour change on a block the review calls the cleanest in the repo. The reason to do it anyway is B20: llm replays whatever string is there, and `agent` — the only value the block's own composer offers — is currently replayed as the user. **The alternative** is to fix `role_from_str` alone (add an `"agent" => Assistant` arm) and leave the column untyped; that closes B20 for a two-line diff and no snapshot movement, and leaves the underlying "any string is a role" problem for later. Say which, because it changes PR 3's size by roughly half.

## 6. Non-goals

- Runtime lifecycle and adapters: `pipeline.rs`'s dropped request-log row, `impresspress-cloudflare/src/config_service.rs`'s silent `set`, `impresspress-bundle`'s swallowed `remove_file`, the `stripe.rs` split and `handle_webhook`'s 1125 lines, a Worker `scheduled` handler. **Phase 4.**
- Every upstream item this phase discovered, recorded and not done: `config::get_bool` in `wafer-core` (5.4); an optional detail code on `wafer_block`'s `err_*` (5.3); a `DbExec` forwarder macro. **Phase 4.**
- The `_cents` → `_minor` column and wire rename in full, Stages A, B and C (2.3.a, 2.3.1–2.3.5). Deliberately deferred by ruling 2 to its own release, with its own `RELEASE.md` note. This phase does the correctness half only (2.3.6, 2.3.7) and renames no column.
- Rendering `tickets::ENDPOINT_REFERENCE` and legalpages' endpoints page from `ROUTES`; the admin API-key revoke swap; `auth_method` on the device list. **Phase 5.**
- CI workflow reuse, regenerating SDK types from `/openapi.json`, one `HttpClient`. **Phase 6.**
- Re-opening the seven T4 sites Phase 2 already closed (1.3, first paragraph), and "fixing" the two deliberate anti-enumeration constant responses (`auth_ui/api/verify.rs:120-122`, `forgot_password.rs:26`). Both lists are in the spec so the next sweep does not have to rediscover them.
- Typing `messages.context_type` (5.7), and the `stripe_events`/`provider_operations` **vocabulary merge** — this phase gives each its own enum and leaves the `processed` vs `succeeded` divergence intact, because reconciling them changes stored values.
- Any change to wafer-run at rev `7d47e5e`.
- Splitting `handle_webhook`, or adding a retry loop to the Stripe paths B21 reclassifies. B21 fixes the classification only.
